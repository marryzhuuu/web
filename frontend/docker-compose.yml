version: '3.8'

services:
  frontend:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - ./logs:/usr/local/apache2/logs
    environment:
      - VUE_APP_API_URL=http://localhost:38173/api